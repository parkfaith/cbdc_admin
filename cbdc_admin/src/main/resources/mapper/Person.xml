<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cbdc.admin.systemMng.dao.PersonManageMapper">

	<select id="getPersonList" parameterType="com.cbdc.admin.systemMng.model.PersonManageModel" resultType="com.cbdc.admin.systemMng.model.PersonManageModel">
		SELECT
			USER_SEQ,
			USER_ID,
			USER_PWD,
			USER_NM,
			AUTH_CODE,
			REG_ID,
			DATE_FORMAT(REG_DATE, '%Y-%m-%d') as REG_DATE,
			UPD_ID,
			UPD_DATE
		FROM
			TB_ADM_USER
		WHERE 1=1
		<choose>
			<when test="search_type != null and search_type.equals('user_id')">
				AND USER_ID LIKE CONCAT('%', #{search_keyword}, '%')
			</when>
			<when test="search_type != null and search_type.equals('reg_id')">
				AND REG_ID LIKE CONCAT('%', #{search_keyword}, '%')
			</when>
		</choose>
		ORDER BY USER_SEQ DESC
	</select>

</mapper>